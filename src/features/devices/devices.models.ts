export type DeviceDBModel = {
  ip: string;
  title: string;
  lastActiveDate: Date;
  deviceId: string;
  userId: string;
};
export type DeviceViewModel = {
  ip: string;
  title: string;
  lastActiveDate: string;
  deviceId: string;
};
